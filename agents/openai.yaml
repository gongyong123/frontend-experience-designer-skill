interface:
  display_name: "Frontend Experience Designer"
  short_description: "Project-aware frontend UX, collaboration mode, and stack defaults."
  default_prompt: "Use $frontend-experience-designer to infer the current frontend collaboration mode, classify this frontend project type, resolve blocking architecture decisions before scaffolding, choose the right architecture source, and apply suitable UX, visual, and motion patterns."

policy:
  allow_implicit_invocation: true
